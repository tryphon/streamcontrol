<%= title_tag t(".title") %>

<%= render @streams %>

<div class="inline_help">
  <p>
    <%= render :partial => 'index_inline_help' %>
  </p>
</div>

<% if Stream.can_create? %>
  <div class="actions">
    <%= link_to t(".actions.create"), new_stream_path, :class => "create" %>
  </div>
<% end %>
